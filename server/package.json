{
  "name": "rollon-reference-api",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency reference API server for the RollON storefront (docs/API.md contract)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "generate:seed": "node scripts/generate-seed.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
