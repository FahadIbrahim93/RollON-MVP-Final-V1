# Kilo Task: API Tests
**Project: RollON MVP**

Read src/lib/api.ts and create tests for:
1. api.products.create
2. api.products.update  
3. api.products.delete
4. api.orders.create

The store uses useDatabaseStore. Tests should mock or use the store.

Return test code in src/lib/__tests__/api.test.ts format.