{
  "name": "rollon",
  "version": "1.0.0-beta.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.10.0"
  },
  "description": "RollON — config-driven e-commerce template. Reference API lives in /server, Vercel serverless entrypoint in /api.",
  "scripts": {
    "build": "cd rollon-app && npm run build",
    "test:server": "cd server && npm test"
  }
}
